<p>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devour me!{{else}}Restock{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">X</button>
</p>
